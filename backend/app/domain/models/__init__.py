"""Domain models package.

ORM models live exclusively under ``app.infrastructure.database.models``.
This package is reserved for true domain model classes that capture business
behaviour independently of persistence concerns.  It will be populated in
later phases when domain complexity justifies the separation.
"""
